---
import type { GetStaticPaths } from "astro";
import Page, { getPostPaths } from "../../posts/[...slug].astro";

export const getStaticPaths = (() => getPostPaths("en")) satisfies GetStaticPaths;
---

<Page {...Astro.props} />
